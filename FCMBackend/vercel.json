{
    "builds":[{
        "src":"FCMBackend/wsgi.py",
        "use":"@vercel/python"
        
    }],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"FCMBackend/wsgi.py"
        }
    ]
}